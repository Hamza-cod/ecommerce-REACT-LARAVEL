export const loginSeletore = (({login})=>login.login)
export const productsSelectore = (({products})=>products.products)
export const categoriesSelectore = (({categories})=>categories.categories)
export const ordersSelectore = (({orders})=>orders.orders)